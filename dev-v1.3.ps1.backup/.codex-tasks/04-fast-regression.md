# v1.3 第一批功能快速回归

只做当前 v1.3 第一批改动的回归，发现问题时最小修复。

## 范围
- 访问记录
- 蜘蛛分析
- SEO 报告
- 来源分析
- IP 分析
- 设置 -> 隐私设置
- Referer 来源 URL 悬停详情

## 必查
1. `git diff` 没有意外删除现有功能。
2. 5 个筛选页面的基础筛选和高级筛选都还存在。
3. 高级条件生效时页面不会丢失条件。
4. 现有 GET 参数名没有被改变。
5. 数据库结构没有被改变。
6. radio 使用 checked，不再使用 selected。
7. `ip_mode` 保存、读取、采集链路保持一致。
8. Referer 详情输出全部经过 HTML 转义。
9. 超长 URL 不撑宽表格。
10. 重复 DOM id 检查。
11. `git diff --check`。
12. Node 可用时检查改动 JS。
13. PHP CLI 可用时检查改动 PHP。
14. 不运行与当前改动无关的大范围静态扫描。
15. PHPStan 不是阻断项。

## 输出
给出最终人工浏览器验收清单，并明确哪些测试已自动完成、哪些因本机环境仍需人工确认。

## 不允许
- 不增加新功能。
- 不提交 Git。
