# 验证已完成的 v1.3 第一批改动

这是验证任务，不重新开发已经完成的筛选 UI 和隐私设置修复。

## 已完成内容
- 5 个页面的常用筛选 + 高级筛选
- 高级筛选展开/收起
- 隐私设置 radio 改用 checked
- full/masked 采集逻辑保持原样

## 本任务目标
确认当前代码状态真实可用，为下一项 Referer 来源增强建立稳定基线。

## 检查
1. 读取当前 `git diff`。
2. 确认当前分支仍是 `feature/visit-stats-1.3`。
3. 确认没有意外修改数据库结构。
4. 确认 5 个筛选页面仍使用一致的折叠逻辑。
5. 确认隐私设置：
   - `full` radio 正确输出 checked
   - `masked` radio 正确输出 checked
   - 设置保存/读取字段仍为 `ip_mode`
   - collector 根据 `ip_mode` 分支处理新记录
6. 如果 PHP CLI 可用，对本轮已改 PHP 文件执行 `php -l`。
7. 如果 Node 可用，对本轮已改 JS 文件做语法检查。
8. 执行 `git diff --check`。
9. 若本地 Z-Blog 地址可访问并且具备可自动化的浏览器测试条件，可执行轻量页面检查；否则只输出人工验证清单，不要为了测试引入大型框架。

## 不允许
- 不重新设计 UI。
- 不重复修复已经正确的 radio。
- 不提交 Git。
- 不新增功能。

## 输出
给出“当前基线是否可继续开发”的明确结论。
