# xz_visit_stats 版本路线

## 当前状态

当前版本：**v1.3.0**（第一批改动已完成，发布前整理中）。

v1.3.0 第一批改动在不调整访问日志表结构的前提下，统一后台筛选区域的紧凑布局并增加高级筛选展开/收起，修复隐私设置 radio 回显 Bug，并增强来源 URL/Referer 展示。

后台已提供以下入口：

- 统计概览：`main.php?view=overview`
- 访问记录：`main.php?view=records`
- 蜘蛛分析：`main.php?view=spider`
- SEO 报告：`main.php?view=seo`
- 来源分析：`main.php?view=source`
- IP 分析：`main.php?view=ip`
- 数据维护：`main.php?view=maintenance`
- 实时访问：`main.php?view=realtime`

兼容性说明：保留 v1.2.0 的 GET 参数、表单名称、数据库结构、采集字段和公开函数；`full` / `masked` IP 模式行为保持不变。

发布前验证状态：已完成 `git diff --check`、JavaScript 语法检查和变更 PHP 文件语法检查；浏览器和真实数据库运行时验证仍待执行，不能视为已通过。

## 已完成版本

| 版本 | 主题 | 状态 |
| --- | --- | --- |
| v0.1 | 数据采集 | 已完成 |
| v0.2 | 访问日志 | 已完成 |
| v0.3 | 统计概览 | 已完成 |
| v0.4 | 蜘蛛分析 | 已完成 |
| v0.5 | 来源分析 | 已完成 |
| v0.5.1 | 来源界面优化 | 已完成 |
| v0.6 | IP 分析与基础风控 | 已完成 |
| v0.7 | 数据维护与实时日志 | 已完成 |
| v0.8 | SEO 蜘蛛报告增强 | 已完成 |
| v0.9 | SEO 蜘蛛报告增强 | 已完成 |
| v1.0.0 | 正式发布 | 已发布 |
| v1.1.0 | 产品化优化 | 正式发布候选版 |
| v1.2.0 | 管理能力与 SEO 分析增强 | 正式发布 |
| v1.3.0 | 筛选布局、隐私设置与来源展示增强 | 第一批改动完成，待人工验收 |

## 版本维护规则

- 每个版本以可验收的功能边界组织，不在维护版本中混入未定义的新业务模块。
- 新版本开始前先确认已有时间范围、分页、权限和转义接口可否复用。
- 涉及采集或数据结构的变更须补充安装/升级兼容性验证。
- 每次版本完成后更新 `CHANGELOG.md`、完成 PHP 语法检查并进行核心 SQL 抽查。
- 对外发布前，审查 `plugin.xml` 的版本、说明、发布日期和兼容性声明。
