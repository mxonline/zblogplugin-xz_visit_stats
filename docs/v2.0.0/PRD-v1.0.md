# xz_visit_stats v2.0 正式 PRD v1.0

状态：需求设计稿 v1.0，产品评审已完成；尚未冻结开发范围。

产品定位：**站长访问分析中心**。

> 本版已完成产品评审。评审结论详见 `docs/v2.0.0/PRD-review-v1.0.md`。本文件继续作为 v1.0 基线，不直接在此覆盖评审前的全部需求正文；下一版将生成 `PRD-v1.1.md` 并纳入评审修订。

## 评审后必须修订的 P0

1. Dashboard 默认主 KPI 改为真人访问口径，蜘蛛独立统计；
2. `DurationMs` 对外统一称为“服务端处理耗时”；
3. 今天/昨天/7天/30天及自定义时间统一使用 Z-Blog 站点时区和日界线；
4. 聚合图表必须支持下钻到对应访问记录或详情；
5. 增加数据新鲜度 / 采集状态；
6. 页面分析固定 URL 规范化与 Path 聚合规则；
7. 日级汇总、必要索引、汇总幂等与安全重建提升为 P0 发布基础。

## 评审后 P1 调整

- IP 归属地改为 P1 / 条件能力；
- 高基数“来源 URL → 着陆页”组合分析调整为 P1；
- 新增 CSV 导出、保存筛选视图、IP 地域分析、蜘蛛真实性验证；
- 小时级汇总和高级缓存继续 P1。

## 原 v1.0 需求正文

原始完整 v1.0 需求正文已由本次评审归档为评审基线；后续正式执行以 `PRD-v1.1.md` 为准。

## 当前状态

- 产品定位：通过；
- 7 个一级模块：通过；
- P0 / P1 / P2：已完成评审，需要写入 v1.1；
- v1.3 数据兼容原则：通过；
- 最终数据库 schema：尚未冻结；
- Phase 1 返工方案：待技术设计；
- Phase 2：PRD 冻结前不继续扩展开发。

## 下一步

1. 生成 `PRD-v1.1.md`；
2. 将评审修订写入正式需求；
3. 做最终需求冻结评审；
4. 冻结后进入统计口径与最终数据模型设计；
5. 返工 Phase 1 schema / migration；
6. 再进入 Phase 2 正式开发。
